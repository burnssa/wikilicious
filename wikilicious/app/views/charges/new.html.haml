= form_tag charges_path do
  / The form doesn't pass the amount to the create action, so we have to pass it manually.
  = hidden_field_tag :amount, @stripe_btn_data[:amount]
  %h4 Click the button!
  %script.stripe-button{"data-amount" => "<haml_loud", "data-key" => @stripe_btn_data[:key], :src => "https://checkout.stripe.com/checkout.js", "data-amount" => @stripe_btn_data[:amount], "data-description" => @stripe_btn_data[:description] }